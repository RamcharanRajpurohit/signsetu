[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  TZ = "Asia/Kolkata"
  NODE_VERSION = "18"

# Only keep API redirects
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200